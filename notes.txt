Starting with MDD repo
- Removed excercises directory
- Removed Extras directory
- Cleared files directory besides CML
- cleared inventory directory besides the following:
	cml.yml
	network.yml
	nso.yml
-cleared groupvars besides the following:
	mdd.yml
-cleared mdd-data

Commented out the following in the gitlab-ci
  - clean
  - build-cml
  - build-nso
  - init-nso
	- check
	- load-rollback

Kept the following:
- save rollback
- validate
- update

changed the following under dry_run
- ansible-playbook ciscops.mdd.update -e dry_run=yes 
* This is important. If you change dry-run to no it will push changes

added nso-oc==2.34.0 to requirements.txt
changed https://github.com/model-driven-devops/ansible-mdd.git,jk-dev

Setting up environment
export ANSIBLE_PYTHON_INTERPRETER=${VIRTUAL_ENV}/bin/python
python3 -m venv venv-mdd
. ./venv-mdd/bin/activate
pip3 install -r requirements.txt
deactivate
. ./venv-mdd/bin/activate
ansible-galaxy collection install -r requirements.yml


add to envvars
export NSO_URL=https://nso.cisco.development.smit-th.com:30002
export NSO_USERNAME=admin
export NSO_PASSWORD=aiTh7ush

export ennvars
make sure to install everything in the correct directory.




